import nemo;
import vm_translator;

main() {
    prog = "
var x : int;
var arr: [[[int]]];
{
    arr := [[1,2],[34,52,15]];
    {
        {
            x:=x + 1;
            {(!x < 50) ? U (x>100)?};            
        }
    }*;
    
    ((x >= 100 and x < 100) or 300 < 1000) ?;
    b := upd(arr, 124, (214 + i) * 2);
    print(x+10);
    
}
";
    nemo = s2ast(prog);
    vm = nemo2nvm(nemo);
    printvmProg(vm);
    quit(0);
}
